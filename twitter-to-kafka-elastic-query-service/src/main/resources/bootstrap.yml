spring:
  application:
    name: kafka_to_elastic_query
  profiles:
    active: kafka_to_elastic_query
  cloud:
    config:
      name: elastic-query-service,config-client
      uri: http://localhost:8888