spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: file://Users/heinz/Documents/developments/rws-twitter-to-kafka/twitter-to=kafka-config-server-repository
          clone-on-start: true
    fail-fast: true